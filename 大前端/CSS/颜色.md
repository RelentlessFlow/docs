# CSS颜色

color-mix 语法，可基于普通RGB计算透明度

```
--color-interaction-hover: color-mix(in srgb, #e4e6f1 6%, transparent);
```

oklch 语法，可以实现与 color-mix  一样的语法，需要实现定义变量

```
color: oklch(from var(--base-color) l c h / 6%);
```

